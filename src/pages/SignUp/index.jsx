

import "./style.scss";

const index = () => {
    return (
        <section className="bg-red-500 h-screen w-screen">
            <h1>Sign Up</h1>
        </section>
    );
};

export default index;